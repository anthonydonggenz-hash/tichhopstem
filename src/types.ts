export interface STEMLesson {
  plan: string;
  student: string;
  rubric: string;
  media: string;
}

export interface STEMParams {
  level: string;
  grade: string;
  subject: string;
  book: string;
  topic: string;
  duration: string;
  features: string;
  notes: string;
}
